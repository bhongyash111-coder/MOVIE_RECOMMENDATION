# Render.com configuration for backend
# This file is optional - Render auto-detects Python apps

# Build settings
build:
  - pip install -r requirements.txt

# Start command
start: python app.py

# Environment
env: python

# Health check
healthCheckPath: /api/health
