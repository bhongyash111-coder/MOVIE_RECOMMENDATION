# Nixpacks configuration for Railway
# This project has separate backend and frontend
# Deploy them as separate services

[phases.setup]
nixPkgs = ["python39", "nodejs-18_x"]

[phases.install]
cmds = ["echo 'This is a monorepo. Deploy backend and frontend separately.'"]

[start]
cmd = "echo 'Please deploy backend and frontend as separate Railway services'"
